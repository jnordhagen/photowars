import { fakeUsers } from "../../users";

const comments = [
  {
    author: fakeUsers[0]!._id,
    caption: 'This original photo makes it look so...huggable.',
    votes: []
  },
];

export { comments };